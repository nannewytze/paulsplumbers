<section class="problems-banner">
  <p>Banner will go here.</p>
</section>

<div class="problems-gallery">
  <% @problems.each do |problem| %>
  <div class="problem-card">
  <p><%= problem.name %></p>
  <p class= "problem-image"><%= image_tag problem.image_url %></p>

  <p class="problem-button"> <%= link_to "info about this problem", problem_path(problem.id) %></p>
  </div>
<% end %>
</div>
